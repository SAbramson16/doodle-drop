<section class="section">
  {{#each arts as |art|}}
    <div class="container">
      {{> imagePost
        title=art.title
        description=art.description
        imageUrl=art.imageUrl
        username=art.User.name
      }}
      {{> commentSection artId=art.id comments=art.comments logged_in=../logged_in }}
    </div>
  {{/each}}
</section>